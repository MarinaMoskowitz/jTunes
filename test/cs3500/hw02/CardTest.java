/*
package cs3500.hw02;

import org.junit.Test;

import Card;
import Suit;
import Value;

import static org.junit.Assert.assertEquals;
*/
/**
 * Created by marinamoskowitz on 1/29/17.
 *//*

public class CardTest {

  @Test
  public void toStringTestValid() {
    assertEquals(new Card(Value.A, Suit.SPADES).toString(), "A♠");
    assertEquals(new Card(Value.A, Suit.CLUBS).toString(), "A♣");
    assertEquals(new Card(Value.A, Suit.HEARTS).toString(), "A♥");
    assertEquals(new Card(Value.A, Suit.DIAMONDS).toString(), "A♦");
    assertEquals(new Card(Value.TWO, Suit.SPADES).toString(), "2♠");
    assertEquals(new Card(Value.TWO, Suit.CLUBS).toString(), "2♣");
    assertEquals(new Card(Value.TWO, Suit.HEARTS).toString(), "2♥");
    assertEquals(new Card(Value.TWO, Suit.DIAMONDS).toString(), "2♦");
  }
}
*/
